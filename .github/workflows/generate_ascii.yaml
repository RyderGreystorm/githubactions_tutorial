name: Generate ASCII Artwork
on:
  push:
    branches: [ "main" ]

jobs:
    ascii_job:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Execute shell script
              run: |
                chmod +x
                ./script